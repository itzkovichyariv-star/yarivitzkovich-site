# LIVE PROGRESS — running log (updated after every step)

**For a NEW session:** open Claude Code in `~/Code/yarivitzkovich-site` and say:
> Read docs/LIVE-PROGRESS.md and continue from the last line.

**Yariv's copy-link (GitHub):** https://github.com/itzkovichyariv-star/yarivitzkovich-site/blob/main/docs/LIVE-PROGRESS.md
**Local path:** /Users/yarivitzkovich/Code/yarivitzkovich-site/docs/LIVE-PROGRESS.md

Binding rule (same as family-tasks): during any working session the assistant appends a line here after EVERY meaningful step and commits+pushes it, so this page is always the live state of the project. Newest entries at the bottom.

---
## LOG (newest at bottom)

- 2026-06-11 — Live-progress log created (mirrors the family-tasks pattern Yariv asked to replicate). No active workstream in this repo right now; the next session starts logging here.
- 2026-07-14 — **Admin `/manage/events`: added full geographic/paper breakdown matrix.** Yariv asked to see downloads/visits/returning broken down by city AND by country AND by paper (previously only "Downloads · by paper" + "Visits · by city" existed). Rebuilt `src/components/BreakdownDrawer.tsx`: replaced the old `PaperBreakdown` + `CountryBreakdown` sections with one `BreakdownMatrix` — a **City / Country / Paper dimension switcher** (styled like the Range tabs) that reslices **four metric columns**: Visits, First-time, Returning, Downloads. All client-side; no DB/endpoint change (the `/live/details` payload already carries kind, visitor_class, city, country_name, paper_slug). "By paper" counts only events with a paper_slug (publication-page visits + all downloads); null-city events fall back to their country label; bots excluded. VERIFIED: exact-match SSR check of the real component with mock events across all 3 dimensions × 4 metrics (hand-computed expected == rendered), plus clean `npm run build` (81 pages). Deploy-gate static public cells 06/07/10/13 PASS; cell 01 (home) red is a known astro-dev substrate artifact (`/api/citations` 404 — Pages Functions need `wrangler pages dev`), reproduces on `main`, unrelated (home doesn't import the component). Could NOT do a live-browser click of the switcher (Chrome extension not connected) — dimension render paths proven via SSR, but the onClick wiring wasn't clicked in a browser. Committed + pushed to `main` → Cloudflare Pages auto-deploys (build-time stamp will read `v1.2026.07.14-<newsha>`). **STILL OPEN / next step:** Yariv smoke-tests the live `/manage/events` as owner — click City/Country/Paper, confirm the switcher toggles and real numbers look right on his iPhone + desktop. If the switcher doesn't respond, the only unverified piece is the client onClick handler.
- 2026-07-15 — **Period navigator (Month / Year / All time) across the globe AND the breakdown.** Yariv: "enough visits now — let the lines show by month or the whole period, I'm afraid the screen gets crowded." Decisions (via AskUserQuestion): granularity = **Month + Year + All time**; scope = **everything, including the globe's dynamic arcs + cities**; globe HUD scopes to the selected period too. Built a shared model + control and wired both surfaces:
  - NEW `src/lib/period.ts` — `PeriodValue {type:'month'|'year'|'all', offset}`, `getPeriodBounds()` → unix from/to + label (current period ⇒ to=now; past ⇒ full-period end), `isCurrentPeriod()`, `periodParams()`.
  - NEW `src/components/PeriodNavigator.tsx` — shared control: Month/Year/All-time tabs + `← label →` nav + calendar picker (month grid w/ future months disabled, year list). Used by BOTH LiveGlobe and BreakdownDrawer.
  - NEW `src/components/PeriodSummary.tsx` — compact period-scoped HUD (total/visits/first-time/returning/downloads, countries+continents reached, top countries, top papers) computed from the loaded events; shown on the globe when a Month/Year is picked (All time keeps the full live GlobeHUD).
  - `functions/live/events.js` — now accepts `from`/`to` (calendar bounds) in addition to legacy `range` (mirrors `/live/details`); backward-compatible.
  - `src/components/LiveGlobe.tsx` — replaced the `24h/7d/30d/All` range row with `PeriodNavigator`; events fetch uses period bounds; polls only a LIVE window; HUD = GlobeHUD (All time) or PeriodSummary (month/year). Arcs + city dots auto-scope via the fetched events. Paper filter preserved.
  - `src/components/BreakdownDrawer.tsx` — replaced the rolling `Day/Week/Month/…/All` tabs in the main view with `PeriodNavigator` (shared getPeriodBounds aliased to avoid its LOCAL Compare-mode getPeriodBounds); fetch + GrowthCharts bucketing now period-scoped; Compare mode untouched. (Note: this drops the old rolling Day/Week/Quarter quick-views from the main view — Month/Year/All time only, per the approved decision.)
  VERIFIED: clean `npm run build` (81 pages); endpoint `node --check` OK; PeriodSummary SSR aggregation exact-match to hand-computed (14 events / 9 visits / 6 first-time / 3 returning / 5 downloads / 7 countries / 3 continents / by-country + by-paper correct); **full PeriodNavigator interaction driven in a real browser on the PRODUCTION build** (served `dist/` statically to avoid the astro-dev HMR-through-proxy hydration break) — tab switch, ← nav (Jul→Jun), picker open (Jun selected, Aug–Dec disabled), month select (Mar ⇒ offset −4, full-month bounds, picker closes), All-time ⇒ null bounds; **`/live` loads, globe renders, PeriodNavigator wired (Month ⇒ `← July 2026 →`), paper filter preserved, ZERO console errors**. Deploy-gate public cells 06/07/10/13 PASS. NOT driven with live data: the actual period-scoped re-fetch results + PeriodSummary-on-globe + the owner-gated `/manage/events` fetch (all need live D1 / owner auth) — covered by Yariv's smoke test. Committed + pushed to `main` → Cloudflare Pages auto-deploys. **STILL OPEN / next step:** Yariv smoke-tests on production — on `/live` pick a Month, confirm arcs/cities/HUD scope to it; on `/manage/events` confirm the same navigator scopes the sparklines + breakdown lists.
- 2026-08-12 — **Academic profile links audited end-to-end (Scholar / ORCID / ResearchGate / Web of Science); all four verified PUBLIC; 3 hardening fixes.** Yariv: "make sure all are leading visitors to an available-to-view version and not to a personal login page." **RESULT: none of the four points at a login wall — no defect found.** Verified as a logged-out visitor: **Google Scholar** (full profile — 953 citations, h-17, i10-26, pub list); **ORCID** (employment 3 / education 2 / 46 works, *plus* anonymous `pub.orcid.org/v3.0/.../person` → `visibility: "public"`); **Web of Science** — the one most likely to fail, and it does NOT bounce to institutional sign-in: it renders the free public view (banner "You are accessing a free view of the Web of Science", ResearcherID HMP-6637-2023, h-9, 267 times cited, 10 of 31 documents). Premium extras (beamplot, co-authors, docs past 10) say "Sign In or Register" — a partial-content gate on a viewable page, not a login wall. **ResearchGate**: automated verification was IMPOSSIBLE (RG serves a Cloudflare "Security check required" CAPTCHA to automation, 403s curl/WebFetch) — Yariv confirmed by screenshot that the profile renders fully to a logged-out visitor (48 publications / 9,236 reads / 625 citations, "PhD · Researcher at Ariel University"). NOTE: an earlier claim that RG showed a stale "Kinneret College" affiliation was WRONG — that came from a stale search-engine snippet; the live profile correctly reads Ariel University. Link topology: single source `src/data/site.ts:23-26` → `Footer.astro:100-165` via BaseLayout; live site serves the identical 4 hrefs on `/`, `/about/`, `/he/`, `/publications/` — no stale duplicate anywhere.
  **FIXES (3, all latent — zero visitor-visible change except the JSON-LD):** (1) `Footer.astro` `profileLinks` array omitted `webOfScience`; it only gates `showProfiles`, so WoS would have silently vanished had the other three ever been blanked — added. (2) `src/data/metrics.json` still had the placeholder `user=YOUR_ID` (inert — `about.astro` reads only hIndex/citations) → corrected to the real Scholar id. (3) `BaseLayout` JSON-LD `sameAs` listed only ORCID + Scholar → now `Object.values(site.profiles).filter(Boolean)`, deriving from the single source so it can no longer drift from the footer (empty socials filter themselves out; they auto-join if ever set).
  **VERIFIED:** clean `npm run build` (81 pages); built `dist/index.html` asserted to contain all four `sameAs` URLs **and** the four footer anchors.
  **DEPLOY GATE: 10/13 PASS, 3 FAIL — all three proven PRE-EXISTING via a git-stash baseline** (stashed the 3 files, rebuilt, re-ran: identical failures) **and all three are NON-defects:** `06-publication-detail` = local D1 drift (`/api/citations` → 500 `no such table: citation_cache`; **PROD returns 200 `{citation_count:28}`**); `02-static-pages`→`/live` = same local drift (`/live/totals` + `/live/events` 500 locally, prod fine); `05-prod-smoke`→`PROD-live-events` = **stale test expectation** — prod healthily returns `{from,to,paper,count,events}` with 9 events, but the cell still asserts a `range` key that the endpoint stopped sending when from/to landed on 2026-07-15.
  Yariv chose **"fix the gate first, then push"** rather than override a red gate — so the three reds were run to ground before deploying (next entry).
- 2026-08-12 — **Deploy gate taken from 10/13 to 13/13 GREEN.** All three reds from the entry above were diagnosed to root cause; two were environment, one was a real (if harmless-in-prod) source bug.
  1. **`05-prod-smoke` → `PROD-live-events`: stale test expectation.** The cell asserted `json.range === '24h'`, but `functions/live/events.js` stopped echoing `range` when the period navigator landed (2026-07-15) — it now resolves every request to explicit bounds and returns `{from, to}` (legacy relative `?range=` ⇒ `to` stays null, `from` = now-24h). Rewrote the assertion to check the **window it actually resolved to** (`from` ≈ now-86400 ±300s, `to` null, `events` an array) — that still proves the legacy `range` param is honoured, which is what the cell exists to catch, instead of asserting a key the API no longer sends. Now `from=… (86400s ago), to=null, eventsLen=9` ⇒ 6/6 PASS.
  2. **`06-publication-detail` + `02-static-pages`→`/live`: local D1 had never been migrated.** `/api/citations` 500'd with `no such table: citation_cache`, and `/live/totals` + `/live/events` 500'd the same way. Fixed by applying the migrations to the persist state the gate uses: `npx wrangler d1 migrations apply yarivitzkovich-events --local --persist-to /tmp/wrangler-yariv-state` (all 9 applied 0001→0009). `/live/*` → 200; `/api/citations` → 404 (table exists, no cached row locally) which `audit-lib.mjs:161` **already whitelists**. NOTE: this is LOCAL STATE, not a repo change — a fresh clone will hit the same phantom reds until it runs that command.
  3. **`02-static-pages`→`/live`: protocol-relative texture URLs broke CORS off https (REAL source fix).** `LiveGlobe.tsx` loaded the earth textures from `//unpkg.com/...`. Over the plain-http dev server that resolves to `http://unpkg.com`, whose **301 to https carries no `Access-Control-Allow-Origin`** — Chromium blocks the redirect on a CORS image request and the globe loads untextured (4 console errors / 2 net errors). Verified the asymmetry directly: `https://unpkg.com/...` → 302→200 **with `access-control-allow-origin: *` on both hops**; `http://unpkg.com/...` → 301 with **no** ACAO. Pinned all three constants (`EARTH_DAY_URL` / `EARTH_NIGHT_URL` / `EARTH_BUMP_URL`) to explicit `https://`. **Production was never affected** (the page is https, so `//` already resolved to https) — this only unbreaks local dev + the gate. Confirmed by screenshot: `/live` now renders the fully-textured Blue Marble globe locally.
  **VERIFIED: deploy gate 13/13 PASS, exit 0 — "safe to deploy."** Clean `npm run build` (81 pages) before the gate run.
  Yariv: **"add it"** ⇒ the preflight was built (next entry).
- 2026-08-12 — **Local-D1 preflight added to the deploy gate (audit harness v1.6).** Ports the family-tasks `check-local-migrations.mjs` pattern so the phantom-red class from the entry above can never cost a session again. NEW `scripts/check-local-migrations.mjs`: replays every `CREATE TABLE` in `db/migrations/*.sql` — tracking CREATE/DROP/RENAME in source order, and stripping `--` comments first so a comment containing the words "CREATE TABLE" can't invent a phantom table (that bit family-tasks live on 2026-07-04) — then asserts each resulting table exists in the local D1.
  **The one thing that differs from the family-tasks original, and it's the thing that matters:** this repo does NOT use wrangler's default `.wrangler/state`. Its substrate runs with `--persist-to /tmp/wrangler-yariv-state`, so every `wrangler d1` call in the preflight passes the same flag. Without it the check would inspect a DIFFERENT, empty database and report a cheerful green while the server the cells actually hit was still missing its tables — i.e. the exact failure it exists to prevent, inverted. Overridable via `WRANGLER_PERSIST_TO` (which is also how it was tested without touching the real state).
  Wired into `deploy-gate.mjs` as pre-gate step 1b, **only on the wrangler substrate** (`PORT === 4324`) — the astro-dev fallback never runs Pages Functions or touches D1, so it's skipped there with a printed reason rather than false-failing. Bypass: `--skip-migrations` (documented in the usage header).
  **VERIFIED — every branch exercised, not assumed:** ✅ GREEN on the real persist path *while `wrangler pages dev` held the DB* (8/8 tables — proves no SQLite lock contention with the running substrate); ✅ **RED proven** against an empty throwaway persist path — named all 8 missing tables with their source migration files and printed the exact fix command with the right path interpolated, exit 1; ✅ `--fix` applied 0001→0009 to that throwaway path and a re-check went GREEN (full RED→fix→GREEN cycle); ✅ **the gate itself aborts** — with a broken D1 it exits **1 without running a single cell**; ✅ `--skip-migrations` bypass exits 0. Throwaway state deleted after each test; his real `/tmp/wrangler-yariv-state` was never modified by the tests.
  Also refreshed `scripts/audit/README.md` (the audit handoff doc): documented the pre-gate, and corrected a stale table that claimed **"38 cells across 11 suites"** while the gate actually runs 13 — suites `12-book-chapters` and `13-title-casing` had never been added. Now reads **42 cells across 13 suites + the pre-gate**, tagged v1.6.
  **VERIFIED: deploy gate 13/13 PASS with the preflight running first, exit 0.**
